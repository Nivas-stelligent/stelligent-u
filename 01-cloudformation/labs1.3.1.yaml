Description: Creates an S3 Bucket

Parameters:
  FriendlyName:
    Type: String
    Default: nivas-stelligent

Resources:
  Bucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub ${AWS::Region}-${AWS::AccountId}-${FriendlyName}
